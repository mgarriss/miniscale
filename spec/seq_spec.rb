require 'spec_helper'

describe NoteSeq do
  it 'accepts an array of note symbols when created'
  it 'accepts an array of fixnums when created'
  it 'accepts a root note and a sequence of steps when created'
  it 'supports the union operator'
  it 'supports the intersection operator'
  it 'is Enumerable'
end
